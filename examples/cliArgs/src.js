function something(param) {
    console.log(param);
    console.log("a really really really really really really long string")
}

/*
A block comment spanning
multiple lines
*/

something(/* a long, single-line, block comment next to a function call's arguments */ "argument string");
